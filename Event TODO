FSM
What to do when event

ev_button_pressed
	register order
	if hall order
		send order to other elevators
		receive ack
		send to scheduler
		receive lowest/highest cost
	if cab order
		add to local order list

ev_floor_reached
	if floor = destination
	or
	if floor = order && order_dir = dir
		stop and open door

